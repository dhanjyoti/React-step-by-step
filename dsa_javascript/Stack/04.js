//  4. Peek at the top element of the stack without popping.

const stack = [1, 2, 3]; // Example stack with elements

// Peek at the top element of the stack
const topElement = stack[stack.length - 1];

console.log("Top element of the stack:", topElement);
console.log("Stack remains unchanged:", stack);

