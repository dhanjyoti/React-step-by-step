//  46. Calculate and print the absolute value of a number.  

function absoluteValue(number) {
    return Math.abs(number);
  }
  
  // Example number (replace this with any number)
  let inputNumber = -15;
  
  let result = absoluteValue(inputNumber);
  console.log(`The absolute value of ${inputNumber} is ${result}.`);