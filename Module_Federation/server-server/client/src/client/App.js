import React from "react";

import Content from "./Content";

const App = () => {
  return (
    <div
      style={{
        padding: "1rem",
        borderRadius: "0.25rem",
        border: "4px dashed #fc451e",
      }}
    >
      <Content />
    </div>
  );
};

export default App;
