import React from "react";

const Content = (props) => {
  return (
    <div
      style={{
        padding: "1rem",
        borderRadius: "0.25rem",
        border: "4px dashed #228b22",
      }}
    >
      This is client
    </div>
  );
};

export default Content;
